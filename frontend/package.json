{
  "name": "frontend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "elm-watch hot",
    "serve": "serve -s . -l 5000",
    "hot-serve": "concurrently \"npm run dev\" \"npm run serve\"",
    "build": "npx elm make src/Main.elm --optimize --output=elm.js && mkdir -p dist && cp elm.js dist/ && cp index.html dist/ && cp -r assets dist/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "elm": "^0.19.1-6",
    "elm-watch": "^1.2.6",
    "serve": "^14.2.6"
  }
}
